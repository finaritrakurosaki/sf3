{% extends "TutoBundle::Layout.html.twig" %}
{% block title %}TutoBundle:Matiere:Update{% endblock %}
{% block body %}
    <h1>Mettre à jour Matiere</h1>
    {% block content %}
        {% if  app.request.hasPreviousSession %}
            {% for message in app.session.flashBag.get('notice') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
        {% endif %}
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.nomMatiere, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group">
            {{ form_row(form.edit, {'attr': {'class': 'btn btn-info'}}) }}
        </div>
        {{ form_end(form) }}
    {% endblock %}
{% endblock %}

